# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Node (no build step today, but keep this ready if tooling is added later)
node_modules/
npm-debug.log*

# Local scratch / verification output
_verify/
*.local.*

# Claude Code local-only settings (never commit machine-specific permissions)
.claude/settings.local.json
